package com.cmware.ws;


/**
 * Class for Heroku to start.
 */
public class HerokuStart {

    /**
     * @param args
     */
    public static void main(String[] args) throws Exception {
        // final AnnotationConfigWebApplicationContext applicationContext = new
        // AnnotationConfigWebApplicationContext();
        // // applicationContext.register(WebConfig.class);
        // applicationContext.setConfigLocation("classpath:/applicationContext.xml");
        //
        // final ServletHolder servletHolder = new ServletHolder(new
        // SpringServlet());
        // final ServletContextHandler context = new ServletContextHandler();
        // context.setContextPath("/");
        // context.addServlet(servletHolder, "/*");
        //
        // String webPort = System.getenv("PORT");
        // if (webPort == null || webPort.isEmpty()) {
        // webPort = "8080";
        // }
        //
        // final Server server = new Server(Integer.valueOf(webPort));
        //
        // server.setHandler(context);
        //
        // server.start();
        // server.join();
    }

}
